import generateCSS from '../../../assets/js/blocks/generateCss';

function styling ( props ) { 

    const {
        
        id:block_id,
        classMigrate
    } = props.attributes;

}
export default styling;